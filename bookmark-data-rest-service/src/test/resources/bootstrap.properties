# Disable all services that are not needed by boot integration tests.
# Disabling the cloud-config client requires to be configured in bootstrap.properties!
spring.profiles.active=disable-oauth-resource-server,disable-cloud-config-client,disable-eureka-client
